<UserControl x:Class="BackupUtil.Ui.View.JobListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BackupUtil.Ui.View"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <local:LanguageSelectorView DataContext="{Binding LanguageSelectorViewModel}" HorizontalAlignment="Right" VerticalAlignment="Top" Panel.ZIndex="10" />
        <DataGrid x:Name="JobGrid" Grid.Row="0" AutoGenerateColumns="False"
                  IsReadOnly="True" Margin="0,0,0,10" SelectionMode="Single" ItemsSource="{Binding Jobs}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" />
                <DataGridTextColumn Header="Target Path" Binding="{Binding TargetPath}" Width="80" />
                <DataGridTextColumn Header="Source Path" Binding="{Binding SourcePath}" Width="100" />
                <DataGridCheckBoxColumn Header="Recursive" Binding="{Binding Recursive}" />
                <DataGridCheckBoxColumn Header="Differential" Binding="{Binding Differential}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
