<UserControl x:Class="BackupUtil.Ui.View.LanguageSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:service="clr-namespace:BackupUtil.ViewModel.Service;assembly=BackupUtil.ViewModel"
             xmlns:viewModel="clr-namespace:BackupUtil.ViewModel.ViewModel;assembly=BackupUtil.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200" d:DataContext="{viewModel:LanguageSelectionViewModel}">


    <UserControl.Resources>
        <ObjectDataProvider x:Key="LocalizationService"
                            ObjectType="{x:Type service:LocalizationService}"
                            MethodName="get_Instance" />
    </UserControl.Resources>


    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[langLabel]}"
                   VerticalAlignment="Center"
                   Margin="0,0,5,0" />
        <ComboBox Width="100"
                  SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" ItemsSource="{Binding AvailableLanguages}"
                  DisplayMemberPath="NativeName" />
    </StackPanel>
</UserControl>
